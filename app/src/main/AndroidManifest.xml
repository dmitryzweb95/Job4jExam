<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="job4j.ru.job4jexam">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="job4j.ru.fragmentTask.HostFragmentsActivity" />
        <activity android:name=".activity.ExamActivity" />
        <activity android:name=".activity.HintActivity" />
        <activity android:name=".activity.ResultActivity" />
        <activity android:name="job4j.ru.dialogs.DateActivity" />
        <activity android:name=".activity.ExamUpdateActivity" />
        <activity android:name=".activity.ExamsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>